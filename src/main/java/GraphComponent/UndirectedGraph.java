package GraphComponent;

/**
 * Undirected graph implementation of the subjects.
 * Will be used for:
 *              1. Drawing the corresponding graph
 *              2. Could be used to reverse the nodes, since now subjects only store prerequisites but not in reverse.
 */
public class UndirectedGraph {
}
